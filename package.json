{
  "name": "bdd-scratch",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress open --e2e -b chrome",
    "ismojo": "npx cypress run --browser chrome --spec='cypress/e2e/ismojo/*.feature' --config baseUrl=https://staging.ismojo.com/signup",
    "newurl": "npx cypress open --e2e --browser chrome --config baseUrl=https://staging.ismojo.com/signup"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^23.2.1",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.7",
    "@faker-js/faker": "^10.1.0",
    "cypress": "^15.7.0",
    "lambdatest-cypress-cli": "3.0.45"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": [
      "./cypress/e2e/step_definitions/**/*.{js,ts}"
    ],
    "json": {
      "enabled": true,
      "output": "cypress/reports/json/report.json"
    },
    "html": {
      "enabled": true,
      "output": "cypress/reports/html/report.html"
    },
    "filterSpecs": true,
    "omitFiltered": true
  }
}